class Solution:
    def findContentChildren(self, g: List[int], s: List[int]) -> int:
        s.sort()
        g.sort()
        i = j = 0
        while i < len(s) and j < len(g):
            if s[i] >= g[i]:
                i+=1
            j+=1
        return i